{
  "name": "aws",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "deploy-arch": "npx serverless deploy",
    "force-dev-deployment": "awslocal apigateway get-rest-apis --region us-east-1 --query 'items[0].id' --output text | xargs -I {} awslocal apigateway create-deployment --rest-api-id {} --stage-name dev",
    "deploy": "npm run -s deploy-arch && npm run -s force-dev-deployment"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "serverless": "^3.40.0",
    "serverless-localstack": "^1.3.1"
  }
}
